schema: spec-driven

# Project context (optional)
# This is shown to AI to help understand the project's conventions.
context: |
  Flare is a Svelte 5 gallery of marketing and motion blocks. Live preview. Copy the .svelte. Own look: ink, ember, spotlight, beams, console chrome. It is not a design system, not an npm kit, no CLI, no registry, no shared Button. Do not name other products or sibling projects. Do not define Flare by comparison. Every new behavior starts as an OpenSpec change. Recorte 1 lives in openspec/changes/establish-flare-gallery — amend that folder, do not create a second change for the same gallery. Archive only after merge.
